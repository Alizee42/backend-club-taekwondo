[build]
builder = "nixpacks"
buildCommand = "mvn -B clean package -DskipTests"

[deploy]
startCommand = "java -jar target/club-taekwondo-0.0.1-SNAPSHOT.jar --server.port=$PORT"
restartPolicyType = "ON_FAILURE"
